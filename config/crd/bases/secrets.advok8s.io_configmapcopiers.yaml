---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: configmapcopiers.secrets.advok8s.io
spec:
  group: secrets.advok8s.io
  names:
    kind: ConfigMapCopier
    listKind: ConfigMapCopierList
    plural: configmapcopiers
    singular: configmapcopier
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.summary.targetNamespaces
      name: Namespaces
      type: integer
    - jsonPath: .status.summary.configMapsInSync
      name: In Sync
      type: integer
    - jsonPath: .status.summary.failures
      name: Failures
      type: integer
    - jsonPath: .status.summary.conflicts
      name: Conflicts
      priority: 1
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ConfigMapCopier is the Schema for the configmapcopiers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ConfigMapCopier
            properties:
              rules:
                description: A list of rules for copying configmaps.
                items:
                  description: |-
                    ConfigMapCopierRule is a rule for copying a configmap. Unlike SecretCopier
                    there is no copyAuthorization: ConfigMaps carry no secret material, so
                    distribution is an admin-operated mechanism with no importer handshake.
                  properties:
                    reclaimPolicy:
                      default: Delete
                      description: Reclaim policy for copied configmap.
                      enum:
                      - Delete
                      - Retain
                      type: string
                    sourceConfigMap:
                      description: Reference to the configmap to copy from.
                      properties:
                        name:
                          description: Name of the configmap to copy from.
                          type: string
                        namespace:
                          description: Namespace of the configmap to copy from.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    targetConfigMap:
                      description: Target configmap to copy to.
                      properties:
                        labels:
                          additionalProperties:
                            type: string
                          description: Labels to apply to the configmap.
                          type: object
                        name:
                          description: |-
                            Name of the configmap to copy to. Defaults to the source configmap's
                            name when empty.
                          type: string
                      type: object
                    targetNamespaces:
                      description: Target namespaces to copy to.
                      properties:
                        labelSelector:
                          description: List of namespaces to match by label.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                        nameSelector:
                          description: List of namespaces to match by name.
                          properties:
                            matchNames:
                              description: List of names to match on.
                              items:
                                type: string
                              type: array
                          required:
                          - matchNames
                          type: object
                        ownerSelector:
                          description: List of namespaces to match by owner.
                          properties:
                            matchOwners:
                              description: List of owners to match on.
                              items:
                                description: OwnerReference is a reference to an owner.
                                properties:
                                  apiVersion:
                                    description: API version of the owner.
                                    type: string
                                  kind:
                                    description: Resource kind of the owner.
                                    type: string
                                  name:
                                    description: Name of the owner.
                                    type: string
                                  uid:
                                    description: UID of the owner.
                                    type: string
                                required:
                                - apiVersion
                                - kind
                                - name
                                - uid
                                type: object
                              type: array
                          required:
                          - matchOwners
                          type: object
                        uidSelector:
                          description: List of namespaces to match by UID.
                          properties:
                            matchUids:
                              description: List of UIDs to match on.
                              items:
                                type: string
                              type: array
                          required:
                          - matchUids
                          type: object
                      type: object
                  required:
                  - sourceConfigMap
                  type: object
                type: array
            type: object
          status:
            description: status defines the observed state of ConfigMapCopier
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ConfigMapCopier resource.
                  Each condition has a unique type and reflects the status of a specific
                  aspect of the resource. The status of each condition is one of True,
                  False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  observedGeneration is the .metadata.generation last processed by the
                  controller. When it is less than .metadata.generation the status does
                  not yet reflect the current spec.
                format: int64
                type: integer
              rules:
                description: |-
                  rules summarises the outcome of each rule, one entry per rule in spec
                  order. It records counts only - never the individual target namespaces -
                  so its size is bounded by the number of rules, not the size of the
                  cluster.
                items:
                  description: ConfigMapRuleStatus summarises the outcome of a single
                    rule.
                  properties:
                    configMapsInSync:
                      description: configMapsInSync is the number of target configmaps
                        in sync for this rule.
                      type: integer
                    conflicts:
                      description: |-
                        conflicts is the number of target configmap names matched by this rule
                        that are owned by something else and so were left untouched.
                      type: integer
                    failures:
                      description: |-
                        failures is the number of target configmaps that could not be copied for
                        this rule.
                      type: integer
                    sourceConfigMap:
                      description: sourceConfigMap identifies the rule's source configmap
                        as "namespace/name".
                      type: string
                    sourceExists:
                      description: sourceExists is whether the source configmap was
                        found this reconcile.
                      type: boolean
                    targetNamespaces:
                      description: targetNamespaces is the number of namespaces this
                        rule matched.
                      type: integer
                  required:
                  - sourceConfigMap
                  - sourceExists
                  type: object
                type: array
              summary:
                description: summary holds aggregate counts across all rules from
                  the last reconcile.
                properties:
                  configMapsInSync:
                    description: |-
                      configMapsInSync is the number of target configmaps confirmed present
                      and up to date after the last reconcile.
                    type: integer
                  conflicts:
                    description: |-
                      conflicts is the number of target configmap names that already existed
                      and are owned by something else (a different ConfigMapCopier, a
                      different source, or not created by this operator at all) and so were
                      left untouched.
                    type: integer
                  failures:
                    description: |-
                      failures is the number of target configmaps that could not be created or
                      updated during the last reconcile.
                    type: integer
                  targetNamespaces:
                    description: |-
                      targetNamespaces is the total number of (rule, namespace) matches across
                      all rules. A namespace matched by two rules counts twice.
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
