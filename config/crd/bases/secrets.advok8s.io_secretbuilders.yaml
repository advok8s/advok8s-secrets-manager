---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: secretbuilders.secrets.advok8s.io
spec:
  group: secrets.advok8s.io
  names:
    kind: SecretBuilder
    listKind: SecretBuilderList
    plural: secretbuilders
    singular: secretbuilder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.generated
      name: Generated
      type: boolean
    - jsonPath: .status.lastGeneratedTime
      name: Last Generated
      type: date
    - jsonPath: .status.nextRotationTime
      name: Next Rotation
      priority: 1
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .status.revision
      name: Revision
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecretBuilder is the Schema for the secretbuilders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SecretBuilder
            properties:
              generator:
                description: |-
                  generator selects exactly one engine - a Starlark script or a gotemplate
                  template - that produces the Secret's data.
                properties:
                  script:
                    description: script is a Starlark program returning the secret
                      = {data, labels, type} global.
                    type: string
                  template:
                    description: 'template is a gotemplate engine: a per-key map of
                      templates producing data values.'
                    properties:
                      data:
                        additionalProperties:
                          type: string
                        description: data maps each output Secret data key to a gotemplate
                          producing its value.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels are merged onto the output Secret (over spec.output.labels). Each
                          value is rendered as a gotemplate.
                        type: object
                      type:
                        description: type optionally overrides spec.output.type. It
                          is rendered as a gotemplate.
                        type: string
                    required:
                    - data
                    type: object
                type: object
                x-kubernetes-validations:
                - message: set exactly one of 'script' or 'template'
                  rule: has(self.script) != has(self.template)
              inputs:
                description: |-
                  inputs are what the generator can read: plaintext constants, referenced
                  Secrets and ConfigMaps, a service account token, operator-generated random
                  material, and (Starlark-only) loadable libraries.
                properties:
                  configMaps:
                    description: |-
                      configMaps references existing ConfigMaps in this namespace for non-secret
                      data. Same shape as secrets.
                    items:
                      description: |-
                        ConfigMapInput binds a handle to one ConfigMap (configMapRef) or a matched set
                        (selector). Exactly one of the two must be set.
                      properties:
                        allowEmpty:
                          description: allowEmpty permits a selector to match zero
                            ConfigMaps (binding an empty list).
                          type: boolean
                        configMapRef:
                          description: configMapRef references exactly one ConfigMap
                            in this namespace by name.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        name:
                          description: |-
                            name is the generator binding handle. It must be a valid identifier
                            (dash-free) and is unique within the configMaps list.
                          pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                          type: string
                        selector:
                          description: selector matches a set of ConfigMaps in this
                            namespace; the handle binds a list.
                          properties:
                            labelSelector:
                              description: LabelSelector matches the resource labels.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                            nameSelector:
                              description: NameSelector matches the resource name
                                (supports globs and "!" exclusions).
                              properties:
                                matchNames:
                                  description: List of names to match on.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - matchNames
                              type: object
                            ownerSelector:
                              description: OwnerSelector matches the resource's owner
                                references.
                              properties:
                                matchOwners:
                                  description: List of owners to match on.
                                  items:
                                    description: OwnerReference is a reference to
                                      an owner.
                                    properties:
                                      apiVersion:
                                        description: API version of the owner.
                                        type: string
                                      kind:
                                        description: Resource kind of the owner.
                                        type: string
                                      name:
                                        description: Name of the owner.
                                        type: string
                                      uid:
                                        description: UID of the owner.
                                        type: string
                                    required:
                                    - apiVersion
                                    - kind
                                    - name
                                    - uid
                                    type: object
                                  type: array
                              required:
                              - matchOwners
                              type: object
                            uidSelector:
                              description: UIDSelector matches the resource UID.
                              properties:
                                matchUids:
                                  description: List of UIDs to match on.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - matchUids
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: set exactly one of 'configMapRef' or 'selector'
                        rule: has(self.configMapRef) != has(self.selector)
                      - message: '''selector'' must set at least one sub-selector'
                        rule: '!has(self.selector) || has(self.selector.nameSelector)
                          || has(self.selector.labelSelector) || has(self.selector.ownerSelector)
                          || has(self.selector.uidSelector)'
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  constants:
                    description: |-
                      constants is a free-form map of non-secret, typed configuration the
                      generator can reference. Values arrive with their natural type. It is not
                      OpenAPI-validated or pruned per key (author-controlled literals), and must
                      not hold secret material - those come from secrets or generated.
                    x-kubernetes-preserve-unknown-fields: true
                  generated:
                    description: |-
                      generated declares operator-produced random material (passwords, tokens,
                      keys, certificates, ...). All entropy lives here so the script stays
                      deterministic.
                    items:
                      description: |-
                        GeneratedValue declares one piece of operator-generated material. Exactly one
                        kind must be set.
                      properties:
                        bytes:
                          description: BytesSpec generates raw random bytes, surfaced
                            encoded as a string.
                          properties:
                            encoding:
                              default: base64
                              enum:
                              - base64
                              - base64url
                              - hex
                              - base32
                              type: string
                            length:
                              minimum: 1
                              type: integer
                          required:
                          - length
                          type: object
                        caCertificate:
                          description: CACertificateSpec generates a CA certificate
                            and key.
                          properties:
                            algorithm:
                              default: RSA
                              enum:
                              - RSA
                              - ECDSA
                              - Ed25519
                              type: string
                            commonName:
                              type: string
                            duration:
                              type: string
                            ecdsaCurve:
                              default: P256
                              enum:
                              - P256
                              - P384
                              - P521
                              type: string
                            issuerRef:
                              description: |-
                                issuerRef, when set, makes this an intermediate CA signed by the parent;
                                omitted means a self-signed root.
                              properties:
                                generated:
                                  description: generated names a caCertificate generated
                                    entry in this builder.
                                  type: string
                                secret:
                                  description: secret names an input Secret holding
                                    a CA (tls.crt + tls.key).
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: set exactly one of 'generated' or 'secret'
                                rule: has(self.generated) != has(self.secret)
                            maxPathLen:
                              description: maxPathLen bounds the number of intermediate
                                CAs below this one.
                              type: integer
                            rsaBits:
                              default: 2048
                              type: integer
                            subject:
                              description: CertificateSubject holds optional distinguished-name
                                parts for a certificate.
                              properties:
                                countries:
                                  items:
                                    type: string
                                  type: array
                                localities:
                                  items:
                                    type: string
                                  type: array
                                organizationalUnits:
                                  items:
                                    type: string
                                  type: array
                                organizations:
                                  items:
                                    type: string
                                  type: array
                                postalCodes:
                                  items:
                                    type: string
                                  type: array
                                provinces:
                                  items:
                                    type: string
                                  type: array
                              type: object
                          type: object
                        ecdsaPrivateKey:
                          description: ECDSAPrivateKeySpec generates a bare ECDSA
                            private key (PEM), no certificate.
                          properties:
                            ecdsaCurve:
                              default: P256
                              enum:
                              - P256
                              - P384
                              - P521
                              type: string
                          type: object
                        name:
                          description: |-
                            name is the generator binding handle. It must be a valid identifier
                            (dash-free) and is unique within the generated list.
                          pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                          type: string
                        password:
                          description: PasswordSpec generates a human-facing password
                            with composition rules.
                          properties:
                            allowRepeat:
                              default: true
                              type: boolean
                            charset:
                              description: charset, if set, is a literal alphabet
                                that overrides the class toggles.
                              type: string
                            digits:
                              default: true
                              type: boolean
                            excludeAmbiguous:
                              type: boolean
                            excludeCharacters:
                              type: string
                            length:
                              minimum: 1
                              type: integer
                            lower:
                              default: true
                              type: boolean
                            minDigits:
                              type: integer
                            minLower:
                              type: integer
                            minSymbols:
                              type: integer
                            minUpper:
                              type: integer
                            symbolSet:
                              type: string
                            symbols:
                              default: true
                              type: boolean
                            upper:
                              default: true
                              type: boolean
                          required:
                          - length
                          type: object
                        randomInt:
                          description: |-
                            RandomIntSpec generates a bounded random integer in the inclusive range
                            [min, max].
                          properties:
                            max:
                              format: int64
                              type: integer
                            min:
                              default: 0
                              format: int64
                              type: integer
                          required:
                          - max
                          type: object
                          x-kubernetes-validations:
                          - message: max must be >= min
                            rule: self.max >= self.min
                        rsaPrivateKey:
                          description: RSAPrivateKeySpec generates a bare RSA private
                            key (PEM), no certificate.
                          properties:
                            rsaBits:
                              default: 2048
                              type: integer
                          type: object
                        sshKeyPair:
                          description: SSHKeyPairSpec generates an OpenSSH keypair.
                          properties:
                            algorithm:
                              default: Ed25519
                              enum:
                              - Ed25519
                              - RSA
                              - ECDSA
                              type: string
                            comment:
                              description: comment is the trailing comment on the
                                public key.
                              type: string
                            ecdsaCurve:
                              default: P256
                              enum:
                              - P256
                              - P384
                              - P521
                              type: string
                            rsaBits:
                              default: 2048
                              type: integer
                          type: object
                        tlsCertificate:
                          description: TLSCertificateSpec generates a leaf certificate
                            and key.
                          properties:
                            algorithm:
                              default: RSA
                              enum:
                              - RSA
                              - ECDSA
                              - Ed25519
                              type: string
                            commonName:
                              type: string
                            dnsNames:
                              items:
                                type: string
                              type: array
                            duration:
                              type: string
                            ecdsaCurve:
                              default: P256
                              enum:
                              - P256
                              - P384
                              - P521
                              type: string
                            emailAddresses:
                              items:
                                type: string
                              type: array
                            ipAddresses:
                              items:
                                type: string
                              type: array
                            issuerRef:
                              description: issuerRef, when set, makes this CA-signed;
                                omitted means self-signed.
                              properties:
                                generated:
                                  description: generated names a caCertificate generated
                                    entry in this builder.
                                  type: string
                                secret:
                                  description: secret names an input Secret holding
                                    a CA (tls.crt + tls.key).
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: set exactly one of 'generated' or 'secret'
                                rule: has(self.generated) != has(self.secret)
                            rsaBits:
                              default: 2048
                              type: integer
                            subject:
                              description: CertificateSubject holds optional distinguished-name
                                parts for a certificate.
                              properties:
                                countries:
                                  items:
                                    type: string
                                  type: array
                                localities:
                                  items:
                                    type: string
                                  type: array
                                organizationalUnits:
                                  items:
                                    type: string
                                  type: array
                                organizations:
                                  items:
                                    type: string
                                  type: array
                                postalCodes:
                                  items:
                                    type: string
                                  type: array
                                provinces:
                                  items:
                                    type: string
                                  type: array
                              type: object
                            uris:
                              items:
                                type: string
                              type: array
                            usages:
                              description: usages are the key usages / extended key
                                usages.
                              items:
                                type: string
                              type: array
                          type: object
                        token:
                          description: TokenSpec generates a machine-facing token
                            from a uniform alphabet.
                          properties:
                            alphabet:
                              default: alphanumeric
                              enum:
                              - alphanumeric
                              - lowerAlphanumeric
                              - hex
                              - base32
                              - base64url
                              - custom
                              type: string
                            charset:
                              description: charset is the alphabet when alphabet is
                                "custom".
                              type: string
                            length:
                              description: length is the number of random characters,
                                excluding any prefix.
                              minimum: 1
                              type: integer
                            prefix:
                              description: prefix is prepended verbatim (e.g. "mytool_").
                              type: string
                          required:
                          - length
                          type: object
                        uuid:
                          description: UUIDSpec generates a random UUID.
                          properties:
                            version:
                              default: 4
                              description: version is 4 (fully random) or 7 (time-ordered
                                from the generation time).
                              enum:
                              - 4
                              - 7
                              type: integer
                          type: object
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: set exactly one generated kind
                        rule: '[has(self.password),has(self.token),has(self.randomInt),has(self.bytes),has(self.uuid),has(self.rsaPrivateKey),has(self.ecdsaPrivateKey),has(self.sshKeyPair),has(self.caCertificate),has(self.tlsCertificate)].filter(x,
                          x).size() == 1'
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  libraries:
                    description: |-
                      libraries (Starlark only) declares load()-able modules sourced from a key of
                      a referenced ConfigMap.
                    items:
                      description: |-
                        LibraryReference (Starlark only) declares a load()-able module sourced from a
                        key of a referenced ConfigMap.
                      properties:
                        from:
                          description: from is the handle of an inputs.configMaps
                            entry to source the module from.
                          type: string
                        key:
                          description: key is the data key within that ConfigMap holding
                            the module source.
                          type: string
                        name:
                          description: |-
                            name is the load() module name. It must be a valid identifier (dash-free)
                            and is unique within the libraries list.
                          pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                          type: string
                      required:
                      - from
                      - key
                      - name
                      type: object
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  secrets:
                    description: |-
                      secrets references existing Secrets in this namespace, decoded for the
                      generator. Each entry binds a handle to either one Secret (secretRef) or a
                      set (selector).
                    items:
                      description: |-
                        SecretInput binds a handle to one Secret (secretRef) or a matched set
                        (selector). Exactly one of the two must be set.
                      properties:
                        allowEmpty:
                          description: |-
                            allowEmpty permits a selector to match zero Secrets (binding an empty list)
                            instead of holding generation in AwaitingInput.
                          type: boolean
                        name:
                          description: |-
                            name is the generator binding handle. It must be a valid identifier
                            (dash-free) and is unique within the secrets list.
                          pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                          type: string
                        secretRef:
                          description: secretRef references exactly one Secret in
                            this namespace by name.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        selector:
                          description: selector matches a set of Secrets in this namespace;
                            the handle binds a list.
                          properties:
                            labelSelector:
                              description: LabelSelector matches the resource labels.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                            nameSelector:
                              description: NameSelector matches the resource name
                                (supports globs and "!" exclusions).
                              properties:
                                matchNames:
                                  description: List of names to match on.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - matchNames
                              type: object
                            ownerSelector:
                              description: OwnerSelector matches the resource's owner
                                references.
                              properties:
                                matchOwners:
                                  description: List of owners to match on.
                                  items:
                                    description: OwnerReference is a reference to
                                      an owner.
                                    properties:
                                      apiVersion:
                                        description: API version of the owner.
                                        type: string
                                      kind:
                                        description: Resource kind of the owner.
                                        type: string
                                      name:
                                        description: Name of the owner.
                                        type: string
                                      uid:
                                        description: UID of the owner.
                                        type: string
                                    required:
                                    - apiVersion
                                    - kind
                                    - name
                                    - uid
                                    type: object
                                  type: array
                              required:
                              - matchOwners
                              type: object
                            uidSelector:
                              description: UIDSelector matches the resource UID.
                              properties:
                                matchUids:
                                  description: List of UIDs to match on.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - matchUids
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: set exactly one of 'secretRef' or 'selector'
                        rule: has(self.secretRef) != has(self.selector)
                      - message: '''selector'' must set at least one sub-selector'
                        rule: '!has(self.selector) || has(self.selector.nameSelector)
                          || has(self.selector.labelSelector) || has(self.selector.ownerSelector)
                          || has(self.selector.uidSelector)'
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  serviceAccount:
                    description: |-
                      serviceAccount, when set, mints a bound token for the named ServiceAccount
                      (via the TokenRequest API) for kubeconfig-style outputs.
                    properties:
                      audiences:
                        description: |-
                          audiences sets the token's audiences. Omit for the API server's default
                          audience (the kubeconfig/cluster case).
                        items:
                          type: string
                        type: array
                      expirationSeconds:
                        description: |-
                          expirationSeconds is the requested token TTL. The API server clamps it to
                          [600, its configured maximum].
                        format: int64
                        minimum: 600
                        type: integer
                      serviceAccountRef:
                        description: serviceAccountRef references the ServiceAccount
                          in this namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - serviceAccountRef
                    type: object
                type: object
              output:
                description: |-
                  output shapes the produced Secret. Its name is always the SecretBuilder's
                  name; only the type, labels and annotations are configurable here.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: labels are merged with any the generator returns.
                    type: object
                  type:
                    description: type is the Secret type (defaults to Opaque).
                    type: string
                type: object
              regeneration:
                description: |-
                  regeneration controls if and when the Secret is rebuilt after it is first
                  generated. The default is to generate once and never touch it again.
                properties:
                  onInputChange:
                    description: |-
                      onInputChange re-runs the generator when inputs or spec change, keeping
                      generated material stable (a refresh).
                    type: boolean
                  rotateEvery:
                    description: rotateEvery rotates the material this long after
                      it was generated.
                    type: string
                  rotateGenerated:
                    description: |-
                      rotateGenerated, on a rotate, also re-rolls the random material (otherwise
                      only the generation timestamp advances).
                    type: boolean
                type: object
            required:
            - generator
            type: object
          status:
            description: status defines the observed state of SecretBuilder
            properties:
              conditions:
                description: conditions represent the current state of the SecretBuilder
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              generated:
                description: generated is whether the output Secret has been produced.
                type: boolean
              inputFingerprint:
                description: inputFingerprint folds input revisions/hashes; it drives
                  onInputChange.
                type: string
              lastGeneratedTime:
                description: lastGeneratedTime is when the material was last (re)generated.
                format: date-time
                type: string
              nextRotationTime:
                description: nextRotationTime is when the next rotateEvery rotation
                  is due, when set.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the .metadata.generation last processed.
                format: int64
                type: integer
              observedRegenerateToken:
                description: observedRegenerateToken is the last-handled value of
                  the regenerate annotation.
                type: string
              revision:
                description: revision is a content-derived hash, also stamped on the
                  output Secret.
                type: string
              secretName:
                description: secretName is the name of the produced Secret (always
                  the SecretBuilder's name).
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: name must be at most 230 characters so the companion state Secret
            name fits the 253-character object name limit
          rule: self.metadata.name.size() <= 230
    served: true
    storage: true
    subresources:
      status: {}
